// import React from "react";

// interface IconProps {
//   icon:
// }

// export default function Icon({
//   name,
//   className = "",
//   width = 24,
//   height = 24,
// }: IconProps) {
//   const SvgIcon = icons[name]; // Retrieve the correct SVG based on the `name`

//   if (!SvgIcon) {
//     console.error(`Icon "${name}" does not exist.`);
//     return null;
//   }

//   return <SvgIcon />;
// }
